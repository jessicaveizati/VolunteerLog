package ui.windows;

import com.googlecode.lanterna.gui2.BasicWindow;
import ui.UIController;

public class AddWindow extends BasicWindow {
    public AddWindow(UIController uiController) {
        //TODO:
        //Three buttons: add users, add organizations, and add a new volutneer entry
        //Check for negative hours, make sure the user has all the required fields
        //Save entry to databse
        //Back button to the main menu
    }
}
